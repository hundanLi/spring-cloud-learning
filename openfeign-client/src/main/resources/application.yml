spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        enabled: true

  application:
    name: openfeign-client

server:
  port: 8091
management:
  endpoints:
    web:
      exposure:
        include: '*'
    enabled-by-default: true